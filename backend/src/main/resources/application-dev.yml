# application-dev.yml（開発環境）
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

cookie:
  secure: false
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/multilang-memo
    username: root
    password: password